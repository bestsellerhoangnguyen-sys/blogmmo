# TASK-11 — Day 2 Execution (D2-02 → D2-10)

## D2-02 `/guides` filter + search
- Implemented server-side filter by `q` and `category` query params.
- Data source: Prisma `Guide` + `GuideCategory`.
- Added filter form and guide cards with direct link to detail.
- Check: build OK + route render OK.

## D2-03 `/guides/[slug]` step-by-step + progress bar
- Implemented guide detail route with ordered steps.
- Added client component `GuideProgress` with scroll progress bar.
- Check: build OK + detail route generated.

## D2-04 API CRUD post + guide (auth guard)
- Added CRUD routes:
  - `/api/posts`, `/api/posts/[id]`
  - `/api/guides`, `/api/guides/[id]`
- Added auth guard with `requireAuth()`.
- Check: unauthorized mutation returns `401` (verified by curl).

## D2-05 Admin panel `/admin`
- Added admin page with:
  - Create post
  - Publish/Unpublish post
  - Delete post
  - Create guide
  - Publish/Unpublish guide
  - Delete guide
- Data loaded from API routes.
- Check: build OK, no TS errors.

## D2-06 Rich text/MDX editor
- Added `components/mdx-editor.tsx` (Markdown/MDX textarea + preview using `marked`).
- Integrated in admin post creation flow.
- Check: build OK.

## D2-07 Upload image
- Added `/api/upload` to store file into `/public/uploads`.
- Admin UI supports upload and auto-insert markdown image link into editor content.
- Check: route compiled in build.

## D2-08 Rate limiting
- Added `middleware.ts` with in-memory API rate limiting (`120 req / 60s / ip+path`).
- Check: middleware compiled in build output.

## D2-09 Security headers + CSRF
- Added security headers in middleware:
  - `X-Frame-Options`
  - `X-Content-Type-Options`
  - `Referrer-Policy`
  - `Content-Security-Policy`
- Added CSRF helper with cookie token + header verification for mutations.
- Check: curl `/` includes headers; unauthorized mutation returns `401`.

## D2-10 Staging env template
- Updated `.env.example` with local + staging + prod style keys.
- Check: reviewed and committed.

## Day 2 validation summary
- `npm run test` ✅
- `npm run build` ✅
- `curl` header/security check ✅
- Unauthorized mutation check (`POST /api/posts`) ✅ `401`
