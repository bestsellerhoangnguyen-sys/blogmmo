# TASK-05 — D1-01 → D1-03 (Next.js init + Prisma schema + seed)

## Scope
- D1-01: Init Next.js 14 (App Router), push GitHub
- D1-02: Cài Prisma, tạo schema `User + Post + Tag`
- D1-03: Đồng bộ DB và seed 3 post mẫu

## Kết quả triển khai

### D1-01
- Đã khởi tạo Next.js 14 App Router + TypeScript + Tailwind + ESLint.
- Đã giữ nguyên hệ thống `docs/` và `tasks/` hiện có.
- Đã push repo thành công lên GitHub:
  - `git@github.com:bestsellerhoangnguyen-sys/blogmmo.git`
  - Branch: `main`

### D1-02
- Đã cài `prisma` + `@prisma/client`.
- Tạo schema tại `prisma/schema.prisma` với các model:
  - `User`
  - `Post`
  - `Tag`
- Quan hệ:
  - User 1-n Post
  - Post n-n Tag

### D1-03
- Do môi trường DB không cho phép `migrate dev` (shadow DB), đã dùng `prisma db push` để sync schema an toàn cho phase bootstrap.
- Đã tạo `prisma/seed.ts` và seed thành công 3 bài viết mẫu.
- Verify bằng Prisma Client: `post_count=3`.

## Files chính
- `prisma/schema.prisma`
- `prisma/seed.ts`
- `.env.example`
- `package.json` (thêm scripts db)

## Checklist
- [x] Next.js 14 khởi tạo thành công
- [x] Push GitHub thành công
- [x] Schema User/Post/Tag có đủ
- [x] DB sync thành công
- [x] Seed 3 posts thành công

## Ghi chú kỹ thuật
- Sử dụng deploy key để push GitHub (bảo mật theo repo).
- Với quyền DB hiện tại (`appuser`), `prisma migrate dev` bị chặn do thiếu quyền tạo shadow DB.
- Hướng xử lý ở giai đoạn deploy chính thức (D3): dùng `prisma migrate deploy` với migration SQL đã quản lý version.
