# TASK-12 — Day 3 Progress (D3-01 → D3-07)

## D3-01 GitHub Action deploy workflow
- Added `.github/workflows/deploy.yml`.
- Pipeline includes: checkout, setup node, install, test, build, SSH deploy script.
- Status: **Implemented in code**; requires GitHub repo secrets to activate runtime deploy.

## D3-02 PM2 ecosystem + startup
- Added `ecosystem.config.js`.
- App `blogmmo` running under PM2 and saved (`pm2 save`).
- Status: **DONE**.

## D3-03 Nginx reverse proxy
- Nginx vhost configured for HTTPS domain -> `127.0.0.1:3000`.
- Includes proxy for Next app/static.
- Status: **DONE**.

## D3-04 DB deploy step
- On VPS deploy flow executes `npx prisma db push` (schema sync) and `npx prisma generate`.
- Note: migration-based `prisma migrate deploy` is pending once versioned migrations are introduced.
- Status: **PARTIAL (MVP strategy)**.

## D3-05 Backup cron daily
- Configured `/home/deploy/bin_backup_appdb.sh`.
- Cron set: `0 2 * * *`.
- Manual run created backup file under `/backups`.
- Status: **DONE**.

## D3-06 Smoke test routes
- Verified 200 OK:
  - `/blog`
  - `/guides`
  - `/admin`
  - `/api/health`
- Status: **DONE**.

## D3-07 UAT baseline
- Baseline flows available for login/create/publish/delete via `/admin`.
- Full manual UAT checklist pending human run-through.
- Status: **PARTIAL (ready for tester pass)**.
