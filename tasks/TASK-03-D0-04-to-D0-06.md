# TASK-03 — D0-04 → D0-06 (DB + Nginx + PM2)

## Scope
- D0-04: Cài PostgreSQL 16, tạo `appuser` + `appdb`
- D0-05: Cài Nginx, test default page trả HTTP 200
- D0-06: Cài PM2 global, kiểm tra pm2 startup

## Kết quả triển khai

### D0-04 PostgreSQL 16
- Đã cài: `postgresql`, `postgresql-server`, `postgresql-contrib` (version 16.13)
- Đã init DB cluster: `postgresql-setup --initdb`
- Đã enable service: `systemctl enable --now postgresql`
- Đã tạo role/db:
  - user: `appuser`
  - db: `appdb`
- Đã chỉnh `pg_hba.conf` từ `ident` sang `scram-sha-256` cho loopback.
- Verify:
  - `psql -h 127.0.0.1 -U appuser -d appdb` -> `appuser|appdb`

### D0-05 Nginx
- Đã cài `nginx`
- Đã enable/start service
- Verify: `curl -I http://127.0.0.1` -> `HTTP/1.1 200 OK`

### D0-06 PM2
- Đã cài PM2 global (`pm2 -v` = `6.0.14`)
- Đã cấu hình startup service `pm2-deploy`
- Unit mặc định PM2 bị fail trên AlmaLinux 10 (`Type=forking` + PID file), đã fix sang `Type=simple` + `pm2 resurrect --no-daemon`
- Verify: `systemctl is-active pm2-deploy` -> `active`

## Ghi chú bảo mật/ops
- Mật khẩu DB `appuser` được lưu tại VPS: `/root/appdb_appuser.password` (permission 600)
- Khuyến nghị bước tiếp theo: đưa secret vào `.env` app và xoay vòng mật khẩu sau khi deploy lần đầu

## Checklist hoàn thành
- [x] PostgreSQL 16 cài và chạy
- [x] appuser/appdb tạo thành công
- [x] Nginx chạy và trả 200
- [x] PM2 cài xong và startup active
