# TASK-02 — D0-01 → D0-03 (VPS Prep khởi động)

## Scope
- D0-01: Tạo VPS, cấu hình SSH key, tắt root login
- D0-02: Update OS, cài git/curl/wget/unzip
- D0-03: Cài Node.js 20 LTS (nvm) + pnpm

## Yêu cầu đầu vào
- IP VPS
- User SSH ban đầu
- Private key hoặc quyền truy cập SSH key
- Domain dự kiến

## Lệnh triển khai (tham chiếu)
```bash
# D0-02
sudo dnf -y update
sudo dnf -y install git curl wget unzip

# D0-03 (nvm + node20 + pnpm)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
source ~/.bashrc
nvm install 20
nvm alias default 20
corepack enable
corepack prepare pnpm@latest --activate
node -v
pnpm -v
```

## Kiểm tra hoàn thành
- [x] SSH key auth thành công (`deploy` dùng key)
- [x] Root login disabled + password auth disabled (`sshd_config.d/50-hardening.conf`)
- [x] Packages hệ thống đã cài (`git/curl/wget/unzip`)
- [x] Node version = 20.x (`v20.20.2`)
- [x] pnpm available (`10.33.0`)

## Ghi chú triển khai thực tế
- Đã tạo user triển khai: `deploy` (thuộc `wheel`, sudo NOPASSWD)
- Đã cài `nvm`, Node.js 20 LTS và `pnpm` cho cả `root` và `deploy`
- Đăng nhập `root` bằng password đã bị chặn theo D0-01

## Expected output
- Biên bản checkpoint D0-01..D0-03 + command output
