# TASK-13 — Day 3 completion (D3-08 → D3-10)

## D3-08 Lighthouse CI
- Added config `.lighthouserc.json` with assertions:
  - Performance >= 0.90
  - Accessibility >= 0.85
- Added workflow `.github/workflows/lighthouse.yml` to run LHCI on main/workflow_dispatch.
- Current status: workflow configured in repo; runtime execution depends on GitHub Actions run.

## D3-09 Analytics integration
- Added analytics injection component `components/analytics.tsx`.
- Wired in `app/layout.tsx`.
- Added env keys in `.env.example`:
  - `ANALYTICS_SCRIPT_URL`
  - `ANALYTICS_WEBSITE_ID`
- Supports Umami/Plausible-style script loading without code change.

## D3-10 Runbook finalize
- Updated `docs/RUNBOOK.md` with:
  - deploy flow on VPS
  - rollback flow (with DB sync step)
  - restore flow (stop/start app around restore)
  - health-check + backup cron verify commands
  - analytics env activation notes

## Production re-verify after changes
- Build on VPS: PASS
- PM2 reload: PASS
- Cron backup line present: PASS
- App online after reload: PASS
